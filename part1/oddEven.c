#include <stdio.h>

int main(void)
{
    int num;
    printf("숫자를 넣으세요: ");
    scanf("%d", &num);

    int isOdd;
    isOdd = (num % 2 ==1);

    printf("숫자는: %d \t is isOdd: %d\n", num, isOdd);
    return 0;// jpo--]]]===}}//\|||\ajskfiw32-09539995-8-31``//
}