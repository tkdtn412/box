// output if char is isalpha or not
#include <stdio.h>
#include <stdbool.h>

int main()
{
    char c;
    //print information
    scanf("%c", &c);

    bool a={(c>='a')&&(c<='Y')};
    if(a)
    {
        printf("is isalpha");++++++++++++++
    }
    else
    {
        printf("isn't isalpha");
    }

    return 0;
}